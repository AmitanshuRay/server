package com.example.server.model;

// template for a server object
// record keyword provides all the necessary getters and setters
public record Server(String id, String name, String language, String framework) {}
